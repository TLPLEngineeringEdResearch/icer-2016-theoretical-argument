// Increment a counter
x = x + 1

// Advance a simulation forward
t = t + dt

// Move an object under uniform velocity
x_position = x_position + displacement

// Accelerate an object along the x-dimension
x_position = x_position + displacement(t)

// Jitter a plot point using a stochastic function
y_position = y_position + random_noise()
